# Look for .kquery files too
config.suffixes.add('.run')

# shorthand for long xpilot settings
config.substitutions.append( 
  ('%xpilotparams', 
   ' -join -texturedWalls no -texturedDecor no -texturedObjects no  -fullColor no -geometry 800x600+100+100 -keyTurnLeft a -keyTurnRight d -keyThrust w localhost ' ) )
config.substitutions.append( 
  ('%xpilotcliverparams', 
   ' -profiler-start-round=-1 -cliver -posix-runtime -libc=uclibc -client-model=xpilot -load="libX11.so" -no-xwindows ' ) )
