# Look for .kquery files too
config.suffixes.add('.run')

# shorthand for long xpilot settings
config.substitutions.append( 
  ('%xpilotparams', 
   ' -join -texturedWalls no -texturedDecor no -texturedObjects no  -fullColor no -geometry 800x600+100+100 -keyTurnLeft a -keyTurnRight d -keyThrust w localhost ' ) )

xpilot_cliver_params = ' -profiler-start-round=-1 -equality-substitution=1 -use-forked-solver=0 -cliver -posix-runtime -libc=uclibc -client-model=xpilot -load="libX11.so" -no-xwindows '
config.substitutions.append( ('%xpilotcliverparams', ' --use-threads=16 ' + xpilot_cliver_params ) )
config.substitutions.append( ('%xpilotcliverparams_st', ' --use-threads=1 ' + xpilot_cliver_params ) )
config.substitutions.append(('%xpilotbc', 'xpilot_llvm34.bc' ) )
config.substitutions.append(('%tetrinetbc', 'tetrinet_llvm34.bc' ) )
